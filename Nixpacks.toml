[phases.setup]
nixPkgs = ["python311", "postgresql_15"]   # libpq needed by psycopg2-binary

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 2 -b 0.0.0.0:$PORT --timeout 120 --log-level warning \"app:create_app('production')\""