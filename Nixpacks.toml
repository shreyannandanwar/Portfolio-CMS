[phases.setup]
nixPkgs = ["python311", "postgresql_15"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 1 -b 0.0.0.0:$PORT --timeout 120 --log-level warning \"app:create_app('production')\""